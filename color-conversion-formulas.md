# 색상 변환 수식 정리

> ColorCalculator.html 구현 기반 · 발표용 설명 문서

---

## 간단 요약

### 한 줄씩

| 단계 | 변환 | 한 줄 설명 |
|------|------|------------|
| ① | sRGB → Linear RGB | 모니터용 보정값을 물리적 빛의 양으로 되돌림 |
| ② | Linear RGB → XYZ | 기기에 종속된 RGB를 기기 독립적 절대 색으로 변환 |
| ③ | XYZ → L\*a\*b\* | 물리 단위를 사람 눈의 감각 단위로 변환 |
| ④ | L\*a\*b\* → L\*C\*h° | 직교좌표(a\*b\*)를 극좌표(채도·색상각)로 변환 |

### 수식만

```
① C_linear = (C_sRGB/255 + 0.055 / 1.055) ^ 2.4      (어두운 영역은 선형 처리)

② [X, Y, Z] = M × [R, G, B]                           (IEC 61966-2-1 행렬)

③ L* = 116 · ∛(Y/Yn) − 16
   a* = 500 · [∛(X/Xn) − ∛(Y/Yn)]
   b* = 200 · [∛(Y/Yn) − ∛(Z/Zn)]

④ C* = √(a*² + b*²)
   h° = atan2(b*, a*) × 180/π
```

### 핵심 상수

| 상수 | 값 | 의미 |
|------|-----|------|
| D65 화이트포인트 | Xn=95.047 Yn=100 Zn=108.883 | 낮 12시 햇빛 기준 |
| ε | 216 / 24389 ≈ 0.00886 | Lab 비선형 구간 임계값 |
| κ | 24389 / 27 ≈ 903.3 | Lab 선형 구간 기울기 |

---

## 전체 흐름

```
모니터에 표시되는 색 (sRGB 숫자)
        ↓  ① 감마 디코딩
    물리적 빛의 양 (Linear RGB)
        ↓  ② 행렬 변환
    CIE XYZ (기기 독립적 색 공간)
        ↓  ③ 비선형 압축
    CIE L*a*b* (인간 지각 기반 색 공간)
        ↓  ④ 극좌표 변환
    CIE L*C*h° (채도·색상각 분리)
```

**핵심 아이디어:** sRGB는 모니터를 위한 숫자이고, L\*a\*b\*는 사람 눈을 위한 숫자입니다. 그 사이를 물리적으로 정확하게 연결하는 것이 이 변환의 목적입니다.

---

## ① 감마 디코딩 — sRGB → Linear RGB

### 왜 필요한가?

모니터의 R=128은 밝기 50%가 **아닙니다.**

브라운관(CRT) 시절, 전압을 2배 올려도 밝기는 2배가 아니라 약 4.5배 밝아지는 물리적 특성이 있었습니다. 이를 보정하기 위해 저장할 때 미리 "밝기를 눌러놓은" 것이 감마 인코딩입니다. 읽어올 때 반대로 펴줘야 합니다.

### 수식

$$C' = \frac{C_{sRGB}}{255}$$

$$C_{linear} = \begin{cases} \dfrac{C'}{12.92} & (C' \leq 0.04045) \\[10pt] \left(\dfrac{C' + 0.055}{1.055}\right)^{2.4} & (C' > 0.04045) \end{cases}$$

역방향 (감마 인코딩, Linear → sRGB):

$$C_{sRGB} = \begin{cases} 12.92 \cdot v & (v \leq 0.0031308) \\[6pt] 1.055 \cdot v^{1/2.4} - 0.055 & (v > 0.0031308) \end{cases}$$

### 직관

| sRGB 값 | Linear 값 | 의미 |
|---|---|---|
| 0 | 0.000 | 완전한 어둠 |
| 128 | 0.216 | 50%가 아니라 21.6% |
| 255 | 1.000 | 완전한 밝음 |

어두운 쪽에 더 많은 숫자를 배정한 것입니다. 사람 눈이 어두운 쪽 변화에 더 민감하기 때문에, 이렇게 하면 255단계를 효율적으로 쓸 수 있습니다.

---

## ② Linear RGB → XYZ

### 왜 필요한가?

RGB는 특정 모니터의 형광체(빨강/초록/파랑)에 묶여 있습니다. 모니터마다 R이 다릅니다.  
**XYZ는 1931년 CIE가 정의한 기기 독립적 절대 색 공간**으로, 어떤 기기에서든 같은 색을 같은 숫자로 표현합니다.

### 수식 (IEC 61966-2-1 공식값, D65 광원 기준)

$$\begin{bmatrix} X \\ Y \\ Z \end{bmatrix} = \begin{bmatrix} 0.4124564 & 0.3575761 & 0.1804375 \\ 0.2126729 & 0.7151522 & 0.0721750 \\ 0.0193339 & 0.1191920 & 0.9503041 \end{bmatrix} \begin{bmatrix} R_{linear} \\ G_{linear} \\ B_{linear} \end{bmatrix} \times 100$$

역방향 (XYZ → Linear RGB):

$$\begin{bmatrix} R \\ G \\ B \end{bmatrix} = \begin{bmatrix} 3.2404542 & -1.5371385 & -0.4985314 \\ -0.9692660 & 1.8760108 & 0.0415560 \\ 0.0556434 & -0.2040259 & 1.0572252 \end{bmatrix} \begin{bmatrix} X/100 \\ Y/100 \\ Z/100 \end{bmatrix}$$

### 직관

- **Y는 밝기(휘도)입니다.** 2행을 보면 G 계수(0.715)가 R(0.213), B(0.072)보다 훨씬 큽니다. 사람 눈이 초록빛에 가장 민감하기 때문입니다.
- X, Z는 색도 성분입니다.
- **D65**는 낮 12시 햇빛 기준 광원입니다.  
  화이트포인트: $X_n = 95.047,\; Y_n = 100.000,\; Z_n = 108.883$

---

## ③ XYZ → CIE L\*a\*b\*

### 왜 필요한가?

XYZ는 물리적으로 정확하지만, **사람이 지각하는 밝기 차이와 수치 차이가 비례하지 않습니다.** 어두운 색에서 Y가 1 바뀔 때와 밝은 색에서 Y가 1 바뀔 때, 사람이 느끼는 차이가 다릅니다.

L\*a\*b\*는 **"수치 거리 = 지각 거리"가 되도록** 설계된 색 공간입니다. ΔE(색 차이 지수)가 여기서 나옵니다.

### 수식

두 상수를 정의합니다:

$$\varepsilon = \frac{216}{24389} \approx 0.008856 \qquad \kappa = \frac{24389}{27} \approx 903.3$$

> 이 숫자들은 임의의 값이 아닙니다. $(6/29)^3 = \varepsilon$이고 $(29/6)^3 = \kappa$입니다.  
> 선형 구간과 비선형 구간을 **값과 기울기가 모두 연속**이 되도록 만드는 유일한 해입니다.

비선형 함수 $f(t)$:

$$f(t) = \begin{cases} \sqrt[3]{t} & (t > \varepsilon) \\[6pt] \dfrac{\kappa t + 16}{116} & (t \leq \varepsilon) \end{cases}$$

> 어두운 영역에서 세제곱근 대신 선형을 쓰는 이유: 세제곱근은 $t \to 0$에서 기울기가 무한대가 되어 수치적으로 불안정해지기 때문입니다.

최종 변환:

$$L^* = 116 \cdot f\!\left(\frac{Y}{Y_n}\right) - 16$$

$$a^* = 500 \cdot \left[f\!\left(\frac{X}{X_n}\right) - f\!\left(\frac{Y}{Y_n}\right)\right]$$

$$b^* = 200 \cdot \left[f\!\left(\frac{Y}{Y_n}\right) - f\!\left(\frac{Z}{Z_n}\right)\right]$$

### 각 축의 의미

| 축 | 의미 | 범위 | +(양) | −(음) |
|---|---|---|---|---|
| L* | 밝기 | 0 ~ 100 | 흰색 (100) | 검정 (0) |
| a* | 빨강–초록 | ±150 | 빨강 | 초록 |
| b* | 노랑–파랑 | ±150 | 노랑 | 파랑 |

무채색(흰/회/검)은 항상 $a^* = 0,\; b^* = 0$입니다.

---

## ④ CIE L\*a\*b\* → L\*C\*h°

### 왜 필요한가?

a\*b\* 직교 좌표계는 수학적으로 편리하지만, 사람이 색을 표현할 때는 "채도가 높다", "색상이 빨갛다"처럼 이야기합니다. L\*C\*h°는 **같은 색을 극좌표로 표현**한 것입니다.

### 수식

$$C^* = \sqrt{a^{*2} + b^{*2}}$$

$$h° = \text{atan2}(b^*,\; a^*) \times \frac{180°}{\pi} \quad \in [0°, 360°)$$

역방향:

$$a^* = C^* \cdot \cos(h°) \qquad b^* = C^* \cdot \sin(h°)$$

### 각 축의 의미

| 축 | 의미 | 특이사항 |
|---|---|---|
| L* | 밝기 | Lab과 동일 |
| C* | 채도 | 0 = 무채색, 클수록 선명 |
| h° | 색상각 | 빨강=0°, 노랑=90°, 초록=180°, 파랑=270° |

> $C^* = 0$ (무채색)이면 $h°$는 정의되지 않습니다.

---

## 검증 결과

구현 코드에서 실제 계산한 값과 CIE 기준값 비교:

| 입력 RGB | L* | a* | b* | 오차 |
|---|---|---|---|---|
| (255, 0, 0) 빨강 | 53.24 | 80.09 | 67.20 | ΔL < 0.001 |
| (0, 255, 0) 초록 | 87.73 | −86.18 | 83.18 | ΔL < 0.005 |
| (0, 0, 255) 파랑 | 32.30 | 79.19 | −107.86 | ΔL < 0.004 |
| (255, 255, 255) 흰색 | 100.00 | 0.00 | 0.00 | 오차 없음 |
| (0, 0, 0) 검정 | 0.00 | 0.00 | 0.00 | 오차 없음 |

라운드트립(RGB → Lab → RGB): 정수 오차 없음 확인.

---

## 한 줄 요약

> sRGB는 모니터를 위한 약속이고, XYZ는 물리 빛의 언어이고, L\*a\*b\*는 사람 눈의 언어입니다.  
> 이 세 언어를 정확히 번역하는 것이 색상 변환의 전부입니다.

---

*기준 표준: IEC 61966-2-1 (sRGB), CIE 15:2004, D65 광원*
